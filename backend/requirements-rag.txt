# Optional regulation-RAG dependencies (not needed to boot the API).
# Install on the onprem/index host that ingests PDFs and serves real search:
#   pip install -r requirements-rag.txt
#
# The API degrades gracefully without these (regulation search -> available=false),
# so the runtime requirements.txt stays lean.

FlagEmbedding>=1.2.10   # bge-m3 dense embeddings (onprem, local)
pdfplumber>=0.11.0      # PDF parsing for ingest (offline/admin tool only)
numpy>=1.26
openpyxl>=3.1          # xlsx 업무매뉴얼 인제스트 (csv는 표준 라이브러리라 불필요)
