# 시연 런북 — 백엔드 연동 시연

시연 30분 전에 이 문서를 위에서부터 훑으면 됩니다.
**로컬 데모로는 재현되지 않는 문제(F2 마이크·F4 메모)를 현장에서 판정하기 위한 도구와 절차**가 핵심입니다.

관련: [DEMO_FEEDBACK_2026-07-24_ACTIONS.md](./DEMO_FEEDBACK_2026-07-24_ACTIONS.md)

---

## 0. 시연 구성 — 시연 PC에서 앱을 직접 실행한다

시연 PC(Windows 노트북)가 프런트와 백엔드를 **자기 안에서** 돌린다. 그래서 접속 주소가
`localhost`가 되고, **마이크는 그냥 열린다** — https 인증서(mkcert)도, LAN IP 확인도 필요 없다.
"LAN에서 마이크가 안 열린다"는 문제는 이 구성에서는 아예 발생하지 않는다.

최초 설치는 이미 만들어 둔 스크립트가 처리한다(자세한 건 [scripts/windows/README.md](../scripts/windows/README.md)):

```powershell
.\scripts\windows\setup-local-ai.cmd   # .venv · Python/Node 의존성 · Ollama/EXAONE · faster-whisper
npm install
Copy-Item .env.example .env
```

`.env`는 온프레미스 엔진을 가리키게 둔다:

```dotenv
VITE_API_BASE_URL=http://localhost:8000
VITE_USE_REAL_DATA_API=true
VITE_DATA_API_PREFIX=/api/v1
```

> **비워 두면 mock(시뮬레이션) 모드로 돈다.** 화면은 정상으로 보이지만 실제 STT·분류가
> 돌지 않으므로, 라이브를 보여줄 자리면 반드시 채워야 한다.

---

## 1. 시연 전 체크 (5분)

| # | 항목 | 확인 방법 | 안 되면 |
|---|---|---|---|
| 1 | **localhost로 접속** | 진단 패널 `MIC › secure = true` | 마이크가 아예 안 열립니다 → §2 |
| 2 | **ARS 안내 음성** | `ls public/demo/ars/*.mp3` 가 7개 | 소리 없이 자막만 흐릅니다 → [녹음 스펙](../public/demo/ars/README.md) |
| 3 | **마이크 권한** | 통화 시작 후 다이나믹 아일랜드 주황 점 점등 | 브라우저 주소창 자물쇠 → 마이크 허용 |
| 4 | **백엔드 분류 응답** | 진단 패널 `분류 › 업무코드`가 `미분류`가 아닌지 | 미분류면 라우팅 실패 (숨기지 않고 그대로 표시됨) |
| 5 | **화자 라벨** | 진단 패널 `통화 › 화자라벨`이 `0 / N`이 아닌지 | 0이면 후처리 게이트가 완화 판정으로 동작 |

### 접속 주소

```
http://localhost:5173/?role=employee    # 직원 콘솔
http://localhost:5173/?role=customer    # 고객 화면
```

> 시연 PC에서 직접 실행하므로 **localhost가 정답이다.** LAN IP로 우회해 열면 마이크가 죽는다(§2).

---

## 2. (예비) LAN에서 다른 기기로 붙어야 할 때

**기본 구성에서는 읽지 않아도 된다.** 시연 PC가 앱을 직접 돌리므로 localhost로 끝난다.

계획이 바뀌어 *다른 기기가 이 PC에 LAN으로 붙는* 구성이 되면 그때만 문제가 생긴다:
브라우저는 마이크(`getUserMedia`)를 보안 컨텍스트(localhost·https)에서만 허용하므로
`http://192.168.x.x`로 열면 **마이크가 죽는다**. 그 경우에만 로컬 인증서가 필요하다.

```bash
brew install mkcert && mkcert -install     # 시스템 신뢰 저장소에 루트 인증서를 심는다
mkcert <LAN-IP> localhost
# vite.config.ts 의 server 에 https: { key, cert } 추가 후 --host 로 기동
```

확인: 진단 패널 `MIC › secure = true`.

---

## 3. 진단 패널 — 현장에서 버그를 잡는 도구

**여는 법**: `Ctrl+Shift+D` (또는 주소에 `?diag=1`)
기본은 꺼져 있습니다. **고객·심사위원 앞에서는 반드시 닫아 두세요.**

값은 100ms마다 갱신됩니다. 통화 중 계속 띄워 두면 됩니다.

### F2 — "마이크가 안 꺼진다"

`MIC` 섹션의 **판정** 한 줄이 결론입니다.

| 판정 | 의미 | 조치 |
|---|---|---|
| `정상 동작 중` | 스트림 살아 있고 레벨 들어옴 | — |
| `누수 — refCount 0인데 트랙 살아 있음` | **release 누락.** 이게 F2의 실체 | `refCount`를 통화 단계별로 기록해 어느 전이에서 안 줄었는지 |
| `비보안 컨텍스트 — http라 차단됨` | 마이크 문제가 아니라 주소 문제 | §2 |
| `NotAllowedError…` | 사용자가 권한 거부 | 브라우저 권한 재설정 |

**보는 순서**: 통화 연결 → `refCount` 증가 확인 → **통화 종료 → `refCount`가 0으로 떨어지고 `track`이 `ended`가 되는지**.
0으로 안 떨어지면 그 시점의 `phase`를 함께 적어 주세요 — 그게 원인 지점입니다.

> **이미 수정된 원인 하나**: `getUserMedia`를 기다리는 동안 취득이 1→0→1로 튀면 스트림이
> 두 개 열리고 먼저 열린 것이 변수에서 밀려나 **영영 정지되지 않았습니다**(= 마이크가 계속 켜짐).
> React StrictMode(개발)의 mount→cleanup→mount가 이 경로를 그대로 탑니다.
> 세대(generation) 가드를 넣어 막았고, A/B로 확인했습니다 — 가드 없으면 살아남은 트랙 1개, 있으면 0개.
> **진단 패널 판정이 여전히 `누수`로 뜨면 이것과는 다른 원인**이니 꼭 알려 주세요.

### F4 — "상담사 메모가 안 된다"

`MEMO` 섹션에서 **개수**가 늘어나는지만 보면 됩니다.
- 타이핑은 되는데 개수가 안 늘어남 → Enter 처리 문제(IME 조합 확정과 충돌)
- 타이핑 자체가 안 됨 → 포커스를 다른 요소가 뺏고 있음

> 참고: 데모 경로 코드는 정상입니다. `ActiveCall.tsx`에 `if (typing) return` 가드가 있어 입력 중 전역 단축키가 먹지 않고, IME 조합도 처리합니다. **라이브 모드에서만 재현되므로 그때 이 패널로 잡아야 합니다.**

### F3 — "후처리가 왜 안 떴나 / 왜 떴나"

`후처리 게이트` 섹션의 **열릴까**가 종료 시 동작을 미리 알려줍니다.

```
진입 = 상담사 연결됨(active) && 상담사·고객 양쪽 발화 최소 1턴
```

- `열릴까 = NO`로 종료 → 후처리가 열리지 않고 **"미상담 종료"로 처리 내역에만** 남습니다 (정상 동작)
- 화자 라벨이 아예 없으면(`화자라벨 0 / N`) 라벨 대신 발화 존재로 판단합니다 — 라벨을 못 받는다고 정상 상담의 후처리가 막히면 안 되기 때문

---

## 4. 시연 중 문제가 생기면 남길 것

나중에 재현하려면 **그 순간의 값**이 필요합니다. 진단 패널을 열고 스크린샷 한 장이면 충분합니다.

1. 진단 패널이 보이게 **전체 화면 스크린샷**
2. 그때의 **phase**와 방금 한 조작 (예: "샵 누른 직후", "종료 버튼 누른 뒤")
3. 브라우저 콘솔 에러가 있으면 함께

---

## 5. 알려진 상태

| 항목 | 상태 |
|---|---|
| 후처리 진입 조건 (F3) | ✅ 게이트 적용 — 미충족 시 "미상담 종료" 기록 |
| 업무코드 표시 | ✅ 분류 결과(`routing.task_code`) 직결. 실패 시 **미분류**로 표시 (기본값으로 덮지 않음) |
| 후처리 저장 | ✅ 처리 내역에 쌓임 (localStorage, 날짜 바뀌면 초기화) |
| 요약 카드 등장 연출 (F1) | ✅ 좌하단 → 중앙 이동 + 라우팅 체인 점등 |
| 마이크 소유권 (F2) | 🟡 소유권 일원화 + **취득 경합 누수 수정 완료**(아래). 잔여분은 라이브 확인 필요 |
| 상담사 메모 (F4) | ⬜ 데모 경로 정상. **라이브 재현 필요** |
| ARS 안내 음성 | ⬜ `public/demo/ars/` 에 mp3 7개 필요 — 대본은 같은 폴더 README |

---

## 6. 시연 시나리오 (권장 순서)

1. **대기** — 시계 화면. "기다리는 콜센터"
2. **접수** — 고객이 전화 → 자연어로 용건 말함 → AI가 실시간 접수
3. **준비** — ★ 요약 카드가 날아와 안착. *"통화가 연결되기 전에 준비가 끝난다"*
4. **통화** — 본인확인 → 스크립트 따라 응대 → 메모
5. **후처리** — 종료와 동시에 결과 자동 작성 → 저장 → **처리 내역에 쌓이는 것까지 보여주기**

3번이 이 제품의 핵심이므로 여기서 한 박자 쉬어 주세요.
