# 백엔드가 준비되면 이 파일을 복사해 `.env` 로 만들고 값을 채우세요.
# 값이 비어 있으면 앱은 자동으로 mock(시뮬레이션) 응답을 사용합니다.

# 백엔드 API 베이스 URL (예: https://api.kda4-k7.example.com)
VITE_API_BASE_URL=

# 각 기능을 실제 백엔드로 붙일지 여부 (true 로 두면 위 URL 로 호출)
VITE_USE_REAL_STT=false
VITE_USE_REAL_SUMMARY=false
VITE_USE_REAL_EMOTION=false
