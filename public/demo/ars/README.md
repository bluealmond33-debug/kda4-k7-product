# ARS 안내 음성 — 녹음 파일 자리

이 폴더에 아래 **12개 파일**을 넣으면 시연에서 AI 안내 음성이 나갑니다.
**파일이 없으면 조용히 텍스트만 흐릅니다**(에러 없음) — 그래서 지금도 시연 자체는 동작하지만, 소리가 없습니다.

- 재생 주체: `src/lib/arsDialogue.ts` (`new Audio("/demo/" + line.audio)`)
- 대본 원본: `src/data/arsScript.ts` — **문구를 바꾸려면 저 파일을 고치고 여기 녹음도 다시** 하세요.
- 형식: mp3 / 모노 / 44.1kHz면 충분합니다. 여성 상담 음성 톤 권장.

> 파일명은 정확히 아래대로여야 합니다. 대소문자·하이픈까지 일치해야 합니다.

---

## 1. 통화 연결 직후 — `connecting` 단계 (greet-1 하나로 고정, ~19초)

카리나 자기소개·No ARS 선언·개인정보 온프레미스 약속·전달 약속·콜백 안심·"# 눌러주세요" 안내를
한 줄에 다 담아서 greet-1 하나로 끝낸다. 이 한 줄만 듣고도 KARI-NA가 뭘 하는 서비스인지
알 수 있게 하는 것이 목표.

**2026-07-29 개정(2차, "더 쎄게") — 길이가 15초→19초로 늘었으니 기존 `greet-1.wav`가 있다면
반드시 재녹음할 것 — 옛 음성이 남아 있으면 새 자막과 어긋난다.**

| 파일 | 길이(목표) | 대사 |
|---|---|---|
| `greet-1.mp3`(`.wav`) | 19초 | 안녕하세요, 키움은행 AI 상담 도우미 카리나입니다. 번호는 누르지 않으셔도 됩니다. 지금 하시는 말씀은 이 건물 밖으로 나가지 않고, 상담사에게 바로 전달됩니다. 통화가 끊기셔도 말씀은 사라지지 않습니다. 편하게 말씀해 주시고, 다 마치시면 우물 정자를 눌러 주세요. |

## 2. 말이 끊긴 뒤 — `confirm` 단계

`confirm-1`은 아직 녹음이 없어 텍스트로만 뜬다(파일 추가하면 자동으로 잡힘).

| 파일 | 길이(목표) | 대사 |
|---|---|---|
| `confirm-1.mp3` (미녹음) | 2.5초 | 더 말씀하실 내용이 있으신가요? |
| `confirm-2.mp3` | 4초 | 말씀이 끝나셨다면 우물정자를 눌러주세요. |

## 3. 요약·전달 완료 — `prep` 단계 (일반)

일반 상담은 3줄(들었음 → 정리중 → 전달완료)을 순서대로 재생한다.

| 파일 | 길이(목표) | 대사 |
|---|---|---|
| `handoff-1.mp3` | 5초 | 고객님께서 말씀하신 내용 잘 들었습니다, 잠시만 기다려 주세요. |
| `handoff-2.mp3` | 4초 | 말씀하신 내용을 정리하고 있습니다, 잠시만 기다려 주세요. |
| `handoff-3.mp3` | 4초 | 상담사에게 내용을 전달했습니다. 곧 연결해드리겠습니다. |

## 4. 요약·전달 완료 — `prep` 단계 (긴급/E 분류 전용)

보이스피싱 등 긴급(E) 건은 3줄 대신 이 한 줄만 짧게 재생한다. 어느 걸 쓸지는
`arsDialogue.ts`가 `vm.prepSge === "E"` 여부로 고른다.

| 파일 | 길이(목표) | 대사 |
|---|---|---|
| `handoff-emergency.mp3` | 3초 | 전담 상담사에게 바로 연결해 드리겠습니다. |

## 5. 본인인증 (생년월일 8자리) — DTMF 이벤트 기반, phase 큐와 별도

이 6개는 `ARS_BY_PHASE`(위 1~4번이 속한 큐)에 없다. `useCallFlow.ts`가 DTMF
자리수 이벤트(`auth_start`/`auth_progress`/`auth_complete`/`auth_incomplete`)에
맞춰 직접 재생을 트리거하며, `auth_policy=REQUIRED`인 업무일 때만 `prep` 진입
시점에 이 흐름이 시작된다.

| 파일 | 길이(목표) | 대사 | 트리거 상황 |
|---|---|---|---|
| `auth-request.mp3` | 10초 | 본인 확인을 위해 생년월일 8자리를 키패드로 눌러주세요. 예를 들어 1985년 3월 7일이면 19850307입니다. | 인증 시작 |
| `auth-all8.mp3` | 6초 | 8자리를 모두 눌러주세요. 연도 4자리, 월 2자리, 일 2자리 순서입니다. | 8자리 다 안 채우고 종료신호(#/*) 누름 |
| `auth-mismatch.mp3` | 6초 | 입력하신 정보가 등록된 정보와 일치하지 않습니다. 다시한번 눌러주세요. | 8자리는 채웠지만 형식이 유효한 생년월일이 아님(13월·32일 등) |
| `auth-birthdate.mp3` | 6초 | 생년월일 8자리를 키패드로 눌러주세요. 태어난 연도 4자리부터 눌러주시면 됩니다. | mismatch 뒤 재입력 안내(auth-request보다 짧게) |
| `auth-done.mp3` | 4초 | 본인 확인이 완료되었습니다. 상담사에게 연결해 드리겠습니다. | 8자리 유효하게 입력 완료 |
| `auth-hard.mp3` | 6초 | 본인 확인이 어려우신 것 같습니다. 상담사가 바로 도와드리겠습니다, 잠시만 기다려 주세요. | 장시간 무응답 등으로 인증 포기 → 상담사 이관 |

---

## 길이가 중요합니다

`sec` 값이 **다음 말풍선이 뜨는 간격**입니다(`arsScript.ts`). 녹음이 표기된 길이보다 길면
음성이 끝나기 전에 다음 자막이 올라가 어긋나 보입니다.

- 녹음이 길어졌으면 → `src/data/arsScript.ts`의 `sec`을 실제 길이에 맞춰 올리세요.
- 반대로 짧아졌으면 → 굳이 줄이지 않아도 됩니다(빈 시간이 조금 생길 뿐).

## 확인 방법

고객 화면(`?role=customer`)에서 통화 버튼을 누르면 1번 그룹이 재생됩니다.
소리가 안 나면:

1. 파일명이 정확한지 (`greet-1.mp3`, 하이픈·숫자 확인)
2. 브라우저 자동재생 정책 — 통화 버튼 클릭에서 시작하므로 보통 통과하지만, 콘솔에
   `NotAllowedError`가 뜨면 페이지를 한 번 클릭한 뒤 다시 시도
